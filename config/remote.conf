# Use internal RC oscillator (fix for faulty external 32kHz crystal)
CONFIG_CLOCK_CONTROL_NRF_K32SRC_RC=y

# Boost BLE TX power for connection reliability
CONFIG_BT_CTLR_TX_PWR_PLUS_8=y

# Clear bonds on startup (development mode)
CONFIG_ZMK_BLE_CLEAR_BONDS_ON_START=y
